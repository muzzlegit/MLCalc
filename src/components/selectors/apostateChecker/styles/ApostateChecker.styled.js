import styled from "@emotion/styled";
import theme from "constants/theme";

export const CheckerBox = styled.div({});
export const CheckerLabel = styled.label({
  marginRight: "8px",
  color: "#89abad",
});
